# Marketplace audit log

Running record of every issue found, fix shipped, and item still owed, kept so later work can be cross-referenced against it. Newest entries at the bottom of each section. Dates are the day the change was deployed to https://www.rarecomicscollectors.com.

## 1. Shipped fixes and improvements

### Platform and deployability (2026-09-04 → 2026-09-05)
- Migrated from the static/SQLite build to PostgreSQL (Neon via Vercel) with migrate + idempotent seed on every build (`scripts/db-deploy.mjs`); first-run `/admin/setup` creates the first super admin.
- Env-free bootstrap: `SESSION_SECRET` / `APP_ENCRYPTION_KEY` fall back to database-generated secrets.
- Rate limiting moved to the shared `RateLimitBucket` table (serverless-safe, atomic upsert, in-memory fallback).
- Background jobs drain on traffic (`driveJobsOnTraffic`, 30 s throttle per instance) so the once-a-day Hobby cron is only a backstop; `/api/jobs/run` accepts `JOBS_SECRET` or `CRON_SECRET`.
- Production audit (commits `ae33bdc`, `9bd042d`): closed an order-confirmation IDOR, open redirects on signed-in login/register, helper functions leaked as server actions, admin-tier escalation; paid/failed/cancel/refund/payout transitions made race-safe (22 integration checks); every admin setting that had no effect now applies (free-shipping threshold, return window, headline, name, favicon, brand colour, guest tracking); storefront error boundaries; mobile overflow/nav fixes.

### International marketplace features (2026-09-05)
- Seller ship-to country list and customs note (`SellerProfile.shipsToJson`, `customsNote`), enforced at checkout.
- Buyer currency and locale preferences honoured server-side; ship-from, customs and ETA shown on buyer order pages; destination analytics on the seller dashboard.

### Payments (2026-09-05)
- Stripe live keys + webhook verified; PayPal live verified (`PAYPAL_ENV` now case-insensitive); PayPal webhook fails closed with 400 instead of 500.
- Bank wire: structured details (beneficiary, bank, account type/number, routing, SWIFT, IBAN) from Admin › Finance › Payment providers or `BANK_*` env, rendered at checkout, confirmation, order page, guest tracking, admin order page and the "awaiting payment" email.

### Custom domain (2026-09-06)
- `*.vercel.app` 308-redirects to the canonical origin (webhooks and cron exempt); canonical/OG/sitemap/robots all on `https://www.rarecomicscollectors.com`; cookies host-scoped; no old-host references left in code.

### Email deliverability (2026-09-06, commit `b825418`)
- Multipart text + HTML on every message; From = marketplace name + the SMTP mailbox (alignment for SPF/DKIM); Reply-To = support address; own Message-ID; explicit envelope sender; misalignment warning at startup.
- RFC 8058 one-click unsubscribe (headers + link + `/api/email/unsubscribe`) on marketing broadcasts only.
- Verified with a live send: SPF PASS, not blocklisted, score 6.8/10 (remaining points are the DKIM/DMARC DNS records below).

### SEO (2026-09-06, commits `9b69d65`, `98b0281`)
- New indexable landing pages `/collections`, `/collections/[slug]`, `/publishers`, `/publishers/[slug]` with unique titles/descriptions/H1s, editorial copy, breadcrumbs, CollectionPage + ItemList JSON-LD, and cross-links from footer, home, store and product pages (footer era links previously pointed at robots-blocked `/store?era=` URLs).
- Product/Offer structured data only on product pages (store page now uses a summary ItemList); collection breadcrumb and rolling `priceValidUntil` on product pages.
- Sitemap rebuilt in `src/lib/sitemap-entries.ts` (adds the new pages, drops the noindex register page); `X-Robots-Tag: noindex` on private areas and non-media API routes.
- IndexNow: key file (originally at the site root; moved to `/indexnow/<key>.txt` on 2026-09-15, see below), pings on every listing change, weekly sync job.
- `GOOGLE_SITE_VERIFICATION` / `BING_SITE_VERIFICATION` env vars render the ownership meta tags.
- Performance: cover scans re-encoded to WebP with day-long cache headers; home grid no longer eager-loads below-the-fold images. Lighthouse mobile (simulated): SEO 100, a11y 100, best practices 100, performance 72 (home) / 75 (store).
- Titles: home no longer repeats the site name; publisher pages no longer read "DC Comics Comics".

### Catalogue import (2026-09-06)
- WooCommerce export (`wc-product-export-30-4-2023`, 60 rows) converted by `scripts/import-woocommerce.mjs` with the profile `scripts/import-profiles/waymaker-2023-04.mjs` into `prisma/data/catalog-imports/waymaker-2023-04.json`; the seed creates 6 consignment sellers and 57 listings idempotently (3 exact duplicate rows dropped: WooCommerce IDs 372, 421, 443).
- Data normalised: year/era/publisher corrected where the export was wrong (e.g. Zip Comics #3 1940 was filed under Silver Age, Hulk #181 under Silver Age, Wow Comics listed "Marvel" as publisher), grader read from the name (Panic #5 is CBCS, not CGC), labels for Signature Series / restored copies, sale price → price with the regular price as `compareAt`, WooCommerce character categories → tags and a "Character" attribute, page quality extracted into an attribute, one listing's "US only" note preserved as an allowed-country restriction.
- Descriptions kept verbatim (HTML stripped, encoding repaired, seller shipping boilerplate removed); five rows whose text was unusable (empty, contradictory grader, or a broken spec dump) got a short factual description.
- The export's photo host (waymakercomics.com) is offline and not archived, so listings use the issue's cover scan instead of the seller's slab photos (48 of 50 issues found; Detective Comics (2011) #20 and War Machine #1 fall back to the gradient plate).

### Storefront fixes found during the import QA pass (2026-09-06)
- Cover plates no longer request a per-product SVG that only exists for the seed catalogue (every non-seed listing produced a 404 per card).
- CBCS "Verified Signature" labels now get the same yellow treatment and badge as CGC Signature Series; product cards strip any label colour suffix, not just "(Yellow)".
- A bare visit to /report explains how to report instead of returning a 404.
- Verified on production after deploy (commit `32193b4`): sitemap lists 74 product and 6 seller URLs, 83/84 import checks and 104/104 SEO checks pass (the one miss is a test-script artefact), unit 42/42, integration 22/22, e2e 18/18 (two specs re-run against the dev server they are written for).

### Analytics (2026-09-08)
- Google tag `GT-NGWX2GTZ` (gtag.js) loads on every storefront page after hydration, production deployment only (`src/components/google-tag.tsx`; `NEXT_PUBLIC_GOOGLE_TAG_ID` overrides the id). Admin pages are excluded.
- Consent Mode v2 defaults: storage denied for EEA/UK/Switzerland visitors (cookieless pings until a future consent banner grants it), granted elsewhere.
- Verified live: gtag.js loads, consent defaults are queued before config, a measurement hit fires to Merchant Center analytics (destination MC-TPG2B1JPZW, `gcs=G111`), `_ga` cookies set outside Europe, nothing loads on admin pages. The GT container has no Google Analytics 4 destination attached, so GA4 reports stay empty until a G- measurement id is added to the tag in Google`s tag settings (or passed as a second config).
- Cookie policy corrected to the real cookies (`rcc_session` is a cookie, not local storage; obsolete `rcc_users`/`rcc_listings` rows removed; currency/locale/2FA cookies and the `_ga` cookies added) and the privacy policy now lists Google Analytics as a processor.

### Purchase tracking (2026-09-08)
- Conversion page: every placed order lands on `/checkout/complete?order=<number>` (card, PayPal, wire, all payment methods); declined or cancelled payments now land on `/checkout/failed?order=<number>` instead of sharing the URL, and each page redirects to the other if the order state does not match. Both are noindex and robots-blocked.
- The confirmation page queues a GA4-shaped `purchase` event on the Google tag dataLayer once per order (transaction_id = order number, value/tax/shipping in USD, payment_type, items with SKU, name, unit price, quantity), deduplicated across refreshes via sessionStorage. The e2e order flow asserts it fires exactly once.

### Google Merchant Center feed (2026-09-08)
- `https://www.rarecomicscollectors.com/google-shopping-feed.xml` — RSS 2.0 with the `g:` namespace, built from the live catalogue by `src/lib/merchant-feed.ts` (route `src/app/google-shopping-feed.xml/route.ts`): every published, non-deleted listing from an approved seller with stock on hand. Regenerated at most every 15 minutes (ISR + CDN cache) and invalidated immediately when a listing is published, edited, hidden or removed (`listingChanged()` also drops the sitemap, collection and publisher caches and pings IndexNow).
- Per item: id = SKU, title (≤150), description (summary + full text + grading facts, ≤5000), canonical link, image_link + additional_image_link, availability, price / sale_price (compare-at as the regular price), condition used, brand = publisher, identifier_exists = no (no GTIN/MPN is ever invented), google_product_category "Media > Books", product_type paths (era, publisher, grader), shipping (cheapest option to the marketplace country, the figure the product page quotes), shipping_weight, external_seller_id + custom_label_0 = seller, product_detail (grade, label, cert number, page quality, publisher, year, era, key issue) and product_highlight.
- Shipping quoted per item is the cheapest option that actually delivers (`cheapestDeliveryOption`); the product page uses the same helper, so "free insured shipping" is never claimed on the strength of the in-person pickup option.
- Listings Google would disapprove are skipped and logged (`[merchant-feed] skipped <sku>: <reason>`): no image, non-positive price, missing SKU/title, duplicate id; the skip list is also written as an XML comment at the end of the feed and in the `x-feed-skipped` header.
- Verified on production (commit `bd868dd`): 72 items, 2 skipped (the two imports without cover art), 40/40 validator checks (well-formed XML, unique ids, every required attribute, 8 items cross-checked against their product pages for price, availability, image and URL, images fetchable); the order-flow e2e proves the purchase event fires exactly once per order.

### Mobile performance (2026-09-09)
- Baseline (Lighthouse 12, mobile, simulated slow 4G): home 43, store 55, product 54, collection 56. Main causes: the Google tag library (150 KB, ~0.8 s main-thread blocking during startup, competing with fonts and covers for bandwidth), web fonts swapping in after first paint (LCP re-candidate and the only layout shift), a session fetch and re-render after hydration, cover scans served at one size, and short cache lifetimes on covers.
- Round one: Google tag deferred to `lazyOnload`; fonts `display: optional` with size-adjusted fallbacks; session DTO rendered into the HTML (`src/lib/auth/session-dto.ts`) so AuthProvider no longer fetches on start; 192/256/384 px cover variants through a next/image loader with honest `sizes` on the hero and product page; `/covers/*` immutable for a year; publisher-chip counts raised from 3.2:1 to 4.9:1 contrast.
- Tried and rejected: `experimental.inlineCss` (Turbopack inlined the stylesheet three times per page: 453 KB HTML / 72 KB gzipped versus a 10 KB cached stylesheet).
- Round one result (Lighthouse mobile): home 43→60, store 55→50 (noise; TBT dominated by the tag), product 54→64, collection 56→61; accessibility 100/100/100/99; CLS 0 everywhere; home FCP 3.1→1.9 s, LCP 6.0→3.5 s.
- Round two: product cards render on the server (`ProductCardView` shared by `ProductCardServer` and the client `ProductCard`; only the two cart buttons hydrate); cover plates are hook-free `<picture>` elements with AVIF + WebP srcsets at 192/256/384/640 px, lazy below the fold, eager + preloaded for the product page LCP image; cards past the first row use `content-visibility: auto`; the Google tag library now loads on first interaction or 3 s after the load event (queue set up immediately; the order confirmation forces an immediate load; `NEXT_PUBLIC_GOOGLE_TAG_EAGER=true` restores startup loading).
- Cover scans are high-entropy halftone art (entropy 7.7 bits): WebP quality 30 only halves a 300 px file and AVIF q50 saves about 27%, so the "compress images" estimate is not achievable without visible loss; right-sizing is what was done.

### Organic SEO and knowledge base (2026-09-15)
- Knowledge base: new `Article` model (migration `20260914233132_knowledge_base`) with slug, question-style title, short answer, Markdown body, topic, tags, characters, titles, publishers, FAQ pairs, related slugs, sources, status, event date. Public routes: `/guides` (hub with topic counts and search; search results are noindex), `/guides/[slug]`, `/guides/topics/[topic]` (paginated, 24 per page; news ordered by event date), `/characters` and `/characters/[slug]` (hubs built from 34 verified first-appearance facts in `src/lib/guides/characters.ts` plus every character tagged on a listing or article).
- Content: 107 articles seeded from `prisma/data/guides/*.json` (grading 15, collecting and FAQ 15, values 9, care 7, characters 34, titles and key issues 14, publishers 8, news 4 — about 27,800 words). Every article states the question in the title, answers it directly in the first paragraph, then explains; dates, creators, issue numbers and sales figures are limited to documented facts (the record-sales table and timeline list only publicly reported auction results and events with month and venue). No hidden text, no keyword stuffing, no cloaking: the HTML is identical for crawlers and visitors.
- Seeding is idempotent by slug and runs during every build (`seedGuides()` in `prisma/seed.ts`); existing articles are left alone so admin edits survive redeploys (`SEED_REFRESH_GUIDES=true` forces a refresh). New published articles queue an IndexNow ping.
- Admin: `/admin/guides` (list, filter, create, edit, publish/unpublish, delete) and `/admin/guides/import` (JSON batch import, up to 1,000 articles or 5 MB per file, same shape as the seed files) behind the new `content.manage` permission (moderator and above). Saves revalidate the guide pages, hubs and sitemaps and ping IndexNow.
- Structured data and crawlability: Article + BreadcrumbList JSON-LD on every guide, FAQPage when the article has FAQs, canonical and Open Graph tags; `/sitemap.xml` is now a sitemap index pointing at `/sitemaps/site.xml` (store, collections, publishers, characters, topics, static pages) and `/sitemaps/guides-N.xml` (2,000 URLs per file, so the index scales past 10,000 articles without code changes); the weekly IndexNow sync covers guides; listing changes also revalidate `/sitemaps/site.xml` and `/characters`.
- Internal linking: "Guides" in the primary navigation, "Collecting guides" and "By character" in the footer, a guides block on the home page, "Guides about this book" on every product page (matched on title and characters first, then publisher, era and grader), era guides on collection pages and publisher guides (history article first) on publisher pages; every article links to related guides, character hubs, the listings it discusses, collections, publishers, services and policies (link checker over all 107 articles: 0 broken of 234 distinct internal links).
- Bugs found and fixed during verification: character hubs keyed by `slugify(name)` split "Riri Williams (Ironheart)" and "Riri Williams" into two hubs (now keyed by the fact slug everywhere); the product-page guide block returned the first four matches in database order (now title matches outrank character matches); article links to the empty `/collections/raw-books` category and the non-existent `/sellers` index were replaced.
- Verified locally against the production build: 36/36 probe checks (sitemap index and children, every route and 404, JSON-LD, canonical, H1, wiring into home/product/collection/publisher pages, admin protected with `X-Robots-Tag: noindex`, all 107 guide pages render on a 390 px viewport with no overflow and no console errors), storefront QA crawl clean, unit 51/51, e2e order-flow and boundaries 5/5, `tsc` and `eslint` clean.
- Verified on production (commit `70f13bb`, deployed 2026-09-15): the build applied the migration and seeded all 107 articles; the same probe passes 36/36 against https://www.rarecomicscollectors.com (sitemap index → `site.xml` 166 URLs + `guides-1.xml` 107 URLs; canonical, title, description and Open Graph tags on guide pages; Article/BreadcrumbList/FAQPage JSON-LD; `/guides?q=` noindex; `/admin/guides` 307 to login with `noindex, nofollow`; every guide page and all 234 distinct internal links resolve). The Merchant Center feed and IndexNow key file are unaffected (feed still 72 items, 2 skipped).
- Follow-up: the home-page block picked four news articles because every seed article shares a publish date; `latestGuides()` now spreads its picks across topics.

### robots.txt (2026-09-15)
- Bing Webmaster Tools reported "Syntax not understood" on line 14, the `Host:` directive (a Yandex extension Next emitted from `host:` in `src/app/robots.ts`). Removed; the file now contains only `User-Agent`, `Allow`, `Disallow` and `Sitemap` lines, all existing rules unchanged, `Sitemap: https://www.rarecomicscollectors.com/sitemap.xml` kept. Verified live with a strict line-by-line parser (known directives only, absolute sitemap URL that answers with XML, no BOM, LF endings): 0 problems.

### IndexNow key `dfc2e1d4b27a4fc58d5ad41e60cd5704` (2026-09-15)
- Requested: key file at `https://www.rarecomicscollectors.com/indexnow/dfc2e1d4b27a4fc58d5ad41e60cd5704.txt` and every submission naming it as `keyLocation`. Done first exactly as specified (commit `f06881b`) and verified: HTTP 200, `text/plain; charset=utf-8`, body exactly the 32-character key with no BOM, quotes, whitespace or newline, any other name a 404, key absent from home, store, guides, robots.txt, sitemap index, both child sitemaps and the Merchant Center feed.
- Live submissions with that keyLocation were then **rejected**: `api.indexnow.org` and `bing.com/indexnow` both answered `422 InvalidRequestParameters: "One or more URLs are not related to your site verified through the keylocation parameter"` for `/`, `/store` and `/guides`, while a URL under `/indexnow/` was accepted (202). This is the protocol's documented rule (indexnow.org/documentation: a key file at `/catalog/<key>.txt` "can include any URLs starting with …/catalog/ but cannot include URLs starting with …/help/"; Option 1, the root, is "strongly recommended"). A key file under `/indexnow/` can therefore never verify the store or the guides.
- Fix: the same handler (`src/app/indexnow/[file]/route.ts`) also serves the key at the site root, `https://www.rarecomicscollectors.com/dfc2e1d4b27a4fc58d5ad41e60cd5704.txt`, through a `next.config.ts` rewrite (only `[a-zA-Z0-9-]{8,128}.txt` names, and only the configured key answers; `robots.txt` is unaffected). Submissions (`submitIndexNow` in `src/lib/indexnow.ts`, used by the `indexnow_ping` and weekly `indexnow_sync` jobs) post `{ host, key, keyLocation, urlList }` with `keyLocation` = the root file; `INDEXNOW_KEY_LOCATION` forces another absolute URL if ever needed and `INDEXNOW_KEY` still overrides the key. The `/indexnow/…` copy stays live for anything that was pointed at it. The server log records the keyLocation of every batch.
- Verified after deployment: both file locations byte-identical to the key; a real JSON batch (`/`, `/guides`, `/store`) with the root keyLocation accepted by api.indexnow.org, the same batch with the `/indexnow/` keyLocation still 422, and Bing's single-URL GET form with the root keyLocation accepted.
- The single-URL browser form (`https://www.bing.com/indexnow?url=…&key=…`) answers 200 with an empty body, so a browser shows a blank page on success and there is no way to see a failure; Bing also answers 202 (not an error) for a wrong key or a placeholder URL. Added **Admin › Catalog › IndexNow (Bing)** (`/admin/indexnow`, permission `content.manage`): paste up to 100 URLs or paths, the same `submitIndexNow` code path runs synchronously and the page shows the endpoint's status with its meaning, the keyLocation used and the URLs sent; it also checks both key files live (200 and contents match), lists the last 15 automatic `indexnow_ping` / `indexnow_sync` jobs with their status, and documents the status codes. Each manual submission is written to the audit log. Verified with Playwright against a production-configured local build (admin sign-in with 2FA, key file "200, contents match", a real submission of `/guides` and `/store` answered by IndexNow, and a foreign-host URL rejected before sending).

### Catalogue import: certifiedcomic.shop CSV (2026-09-15)
- 16 CGC-graded listings from the `certifiedcomic.csv` scrape (columns: image, title, product URL, vendor, price) added through a new converter, `scripts/import-shopify-csv.mjs`, driven by the profile `scripts/import-profiles/certifiedcomic-2026-09.mjs` → `prisma/data/catalog-imports/certifiedcomic-2026-09.json`, seeded idempotently on every build like the first import (SKU `IMP-<Shopify product id>`).
- Facts: the CSV carries no year, page quality, creators, cert number or description, so every listing's details were taken from the source product pages (`/products/<handle>.json`) and from the CGC labels legible in the listing photos — certification numbers (all 16, matching the source SKUs where present), cover dates, creators, key comments (e.g. the New Agents of Atlas first appearances, "Acetate cover" on the Witchblade Wizard Ace Edition, the ComicTom101 & Mill Geek exclusive on Thor #18) and page quality. The only fact added from outside the sources is Tales of Suspense #41 being the third Iron Man appearance. Nothing else was invented; where the label and the source page disagreed (Nebula #1's month) the label wins.
- Pricing: source price less 25 %, rounded to the nearest price ending in .00, .49 or .99 (ties to .99): e.g. $24.99 → $18.99, $34.99 → $26.00, $125.00 → $93.99, $2,699.99 → $2,024.99, $1,799.99 → $1,349.99. The source price is not shown as a compare-at ("was") price, because it was never this store's price.
- Presentation: series title + issue (variants, printings, signatures and exclusives go into the key-issue line, highlights and attributes), era categories (Silver Age for Tales of Suspense #41, Bronze Age for Batman #227, Modern Age for the rest), new publisher pages for IDW Publishing, Top Cow, Image Comics and Boom! Studios, `Universal Blue` / `Signature Series (Yellow)` labels (the two early-holder books note their older label design in the attributes), and the seller's own slab photos: front as the cover plus the back (and label close-ups where the source had them) as gallery images — the seed now accepts a `gallery` array and the photo pipeline produces the usual 192/256/384/640 WebP + AVIF variants (JPEG originals are not committed).
- Seller: one new placeholder seller, "Certified Comic Shop" (`certified-comic-shop`, `certifiedcomicshop@sellers.rarecomicscollectors.com`, US, 2-day handling, USPS Priority per the source's own text). Contact name, handling time and policies are assumed and should be confirmed or the listings reassigned.
- Duplicates: none — no title/issue/cert overlaps with the 74 existing listings, and the converter refuses duplicate slugs or source ids.
- Verified locally against the production build (185/185 checks): every listing page renders with the rounded price, cert number, grade, seller, cover and back photo, Product JSON-LD, all 16 in the Merchant Center feed, seller storefront and the four new publisher pages 200, browser QA at phone and desktop widths clean.
- Verified on production (commit `4dc4f2a`, deployed 2026-09-15): the build seeded the seller and all 16 listings; every listing page passes the same checks live; the Merchant Center feed grew from 72 to 88 items (still 2 skipped); cover variants served as `image/webp` with the one-year immutable cache. The probe's first feed fetch after the deploy briefly saw the previous cached copy, which is the feed's normal 15-minute revalidation, not a missing item.

### Microsoft Clarity (2026-09-15)
- The Clarity snippet (project `yipx69jj3c`) is rendered inside `<head>` on every page by `src/components/clarity-tag.tsx` from the root layout, exactly as Microsoft's snippet (async loader, so it does not block parsing). Production only, like the Google tag, so local and preview sessions are never recorded; `NEXT_PUBLIC_CLARITY_ID` overrides the project id.
- Cookie and privacy policies now list the `_clck` / `_clsk` cookies and name Microsoft Clarity as a recipient of anonymised usage recordings.
- Verified on the production build: the snippet sits in `<head>` once per page, `window.clarity` is defined after load, the loader is fetched from `https://www.clarity.ms/tag/yipx69jj3c` and reporting requests follow, no page errors.
- Not done (by design, "add only the Clarity code"): Clarity is not wired to Consent Mode, so unlike the Google tag it sets its cookies for EEA/UK/CH visitors too; and because it lives in the root layout it also records signed-in account, seller-dashboard and admin sessions (Clarity masks typed text by default, but page text such as customer names in the admin is visible in replays). Either restricting it to the storefront shell or calling `clarity("consent")` behind a banner would close those gaps.

### HipComic CSV scrapes and the catalogue release queue (2026-09-17)
- Source: 99 search-result scrapes of hipcomic.com (`hipcomic.csv`, `hipcomic (1).csv` … `hipcomic (98).csv`; 4,752 rows, 4,690 distinct listings from 41 HipComic sellers). Columns are scraper CSS-class names in 20 different orders, with a few shifted rows, so `scripts/lib/hipcomic-csv.mjs` takes URL, photo, title and price by header name and classifies every other cell by content (shipping, "or Best Offer", "Sponsored", HipComic's own "Approx. $" figure, auction timers).
- Converter: `node scripts/import-hipcomic-csv.mjs <csv|folder>…` → `prisma/data/catalog-queue/hipcomic.json`, `docs/imports/hipcomic-report.md`, `docs/imports/hipcomic-held.csv`. Incremental: earlier listings keep their slug and release day, new files are appended behind them, already-processed listing ids are skipped. Optional CSV columns named publisher / year fill those gaps when a later scrape has them.
- Facts: the scrape has no structured fields, so series, issue, grade, year, page quality, label and certification number come from the seller's title only (`scripts/lib/hipcomic-title.mjs`, unit-tested in `tests/unit/hipcomic-title.test.ts`). Publisher is taken from the title, from a table of series that belong to one publisher (with year ranges where a name changed hands, e.g. Daredevil from 1964, Captain Marvel from 1968), or from other listings of the same series within three years that all agree. Nothing else is inferred: creators stay "Various", there is no invented GTIN/MPN, and the listing pages of the source could not be read (Cloudflare challenges scripted clients, and that was not worked around).
- Result of the first run: **2,442 listings queued, 145 duplicates skipped, 2,165 rows held**. Duplicates: 62 repeated listing ids (page overlap, sponsored repeats), 81 second listings of a slab already queued under the same CGC certification number (two HipComic sellers list the same books; the cheaper listing is kept), 1 certification number already in the catalogue, 1 same seller + title + photo. Held rows (reasons can overlap): publisher not determinable 1,306, no publication year in the title 1,005, signature mentioned without the label type 324, no issue number 45, lots/sets 25, no numeric grade 24, adult-variant wording 13, two publishers named 5, pre-1938 4, auctions 3, two grades 3.
- Cleaning: broken template text (`{product.Pubilcation_Year} {product.Publisher`), seller stock codes (JH6, JM1…), census and marketing claims ("Census = 1", "Highest graded", "movie coming"), letter grades that repeat the number, ALL-CAPS titles, `Spiderman`/`X-men` spellings, "CGC 9. 8" and "CGC 8,0" typos. Truncated titles ("…") lose the dangling fragment; nothing is completed by guesswork.
- Prices: US$ prices are kept exactly. 1,623 queued listings are priced in CA$ at the source; 1,584 use the US$ figure HipComic prints beside the price and 39 (files scraped without that column) are converted at the same scrape's median rate (0.7149). The CA$ price and the method are kept per listing in the queue file (`priceNote`), not shown to customers. No discount, no compare-at price.
- Listings: house listings (no invented seller accounts), SKU/feed id `IMP-<HipComic listing id>`, slug `<series>-<issue>-cgc-<grade>[-ss]` with the listing id appended on a clash, era category from the year, brand = publisher, label Universal Blue / Signature Series (Yellow) / Restored / Qualified only as stated, the seller's remaining title text as listing notes (the "Key issue" badge only when the notes make that kind of claim — 382 listings), certification number on 293. The source marketplace is not named on any page.
- Photos: the listing photo at HipComic's 800 px size → `public/covers/q/<slug>.webp` (≤640 px wide) plus `<slug>-256.webp` for grids; `CoverArt` serves them with a two-step `srcset`. One photo per listing, named after the slug, so photos cannot cross between listings. 4,884 files, 157 MB in the repository (see recommendations).
- **Release queue** (new): `Product.importSource`, `importFile`, `releaseAt` (migration `20260917170000_catalog_release_queue`). The seed creates queued listings as **drafts** in batches of 200 (one existence query per file instead of three per listing) and publishes only those whose day has come; the new `catalog_release` job (`src/lib/catalog/release-queue.ts`) runs just after 00:00 UTC and on the 03:00 cron, re-checks each due draft (title, issue, publisher, year, grade, SKU, price, stock, description, photo, and no listing with the same certification number already on sale), publishes the ones that pass, refreshes store/feed/sitemaps/collections/publishers and queues IndexNow pings; a draft that fails stays a draft with the reason in its moderation note. Schedule: 100 a day, 2026-09-17 → 2026-10-11 (25 days). Drafts are outside `publishedWhere`, so they are not on the storefront, in the sitemap or in the Merchant Center feed until they are buyable.
- Dashboard: **Admin › Catalog › Release queue** (`/admin/release-queue`): totals, next and last batch, per-day schedule, and a filterable table of CSV file, product, scheduled date, publication status and Merchant Center feed status; "Pause daily releases" (setting `catalog.releasePaused`, also under Settings › System) and "Publish what is due now". Both actions are written to the audit log.
- Scale: collection and publisher pages now render the 96 newest cards with the true totals, year span and lowest price computed in the database (they rendered every listing before, which would have meant 1,300+ cards on the Marvel page).
- Verified locally against the production build: 11,213 checks (queue file integrity, all 100 released pages with price, grade, SKU, certification number, photo, canonical, Product JSON-LD, feed entry with the same price / `in_stock` / `used` / no GTIN or MPN, sitemap entry; sampled drafts 404 and absent from feed and sitemap); the release job end to end (next day's 100 made due → 99 published, 1 deliberately broken draft held back with "Release check failed: no price", job rescheduled for 00:05 UTC, feed 189 → 288 items); seed idempotent (second run 0 new); the 91 existing products untouched; dashboard, pause/resume and a product page in Chrome with no page errors.

- Verified on production (commit `e029564`, deployed 2026-09-17): the build migrated the database and seeded all 2,442 queued listings, the first 100 published; 11,213/11,213 checks pass live (all 100 released pages, feed 88 → 188 items with matching price, `in_stock`, `used` and no GTIN/MPN, sitemap 190 store URLs, sampled drafts 404 and absent from feed and sitemap, photos served as `image/webp`). As with earlier deploys, the very first feed fetch after the deploy saw the previous cached copy for a few minutes.

### Listings without a photo (2026-09-18)
- Two listings from the first import had been live with the gradient placeholder instead of a cover since 2026-09-06 (their photo host was offline and GoCollect had no page under the plain issue slug): Detective Comics (2011) #20 and War Machine #1. Both now have the right cover scan — the DC "We Can Be Heroes" blank sketch variant (`detective-comics-20-we-can-be-heroes-blank-var-ed` on GoCollect; the listing is a Frank Miller sketch on that blank cover, so the printed cover is what a scan can show) and War Machine #1 Collector's Edition (foil cover) — registered in `gocovers-map.json` and attached as the product image by the seed, which now backfills a cover found after a listing was created. Both are therefore in the Merchant Center feed too (the feed's "2 skipped" were these two).
- Rule, so it cannot recur: a listing without a photo is never shown. `publishedWhere` (store, product page, collections, publishers, home, sitemap, feed) now requires at least one image; the first-import seed creates cover-less listings as drafts with a moderation note; admin create / edit / moderate / bulk publish / CSV import honour Settings › Listings › "Listings need at least one image" (edit and bulk keep or put such listings out of the store and say so, CSV rows without `image_url` are saved as drafts); seller edits that remove the last photo hide the listing; the release queue already refused them. The admin listings table marks these rows "no photo".
- Verified on production (commit `e6c4735`, deployed 2026-09-18): both pages show their covers (WebP + AVIF variants served), Product JSON-LD carries the image, both are in the Merchant Center feed with `image_link` (feed 188 → 190 items, nothing skipped), and a published listing without a photo is a 404 on the store, absent from the sitemap and the feed (checked against the local production build).

## 2. Still owed by the site owner (cannot be done from the codebase)
- DNS at Namecheap: CNAME `default._domainkey` → `default._domainkey.privateemail.com` (DKIM) and TXT `_dmarc` → `v=DMARC1; p=none; rua=mailto:<mailbox>` (DMARC). Until then mail authenticates on SPF only.
- Google Search Console: verify ownership (HTML-tag value into `GOOGLE_SITE_VERIFICATION`, redeploy), submit `/sitemap.xml` (the index; it lists the store and guide sitemaps).
- Bing Webmaster Tools: verify (meta-tag value into `BING_SITE_VERIFICATION`, redeploy), submit the sitemap.
- Stripe: repoint webhook `we_1UCLmDGgZ8ar330hV5tfiYjy` from the vercel.app host to the live domain; fix the business profile URL (was www.theplanpalette.com).
- PayPal: repoint the webhook to the live domain.
- Bank-wire fields: confirm they are filled in Admin › Finance › Payment providers (or `BANK_*` env).
- Vercel dashboard "Redeploy" has not produced deployments; push an empty commit to apply env changes.
- HipComic-sourced listings (release queue): these are one-of-a-kind slabs that other sellers list on HipComic, and nothing syncs availability — when a book sells there it stays buyable here until someone hides it. Before the queue runs for long, confirm how each order will be supplied, check the source listing (kept per listing as `sourceUrl` in `prisma/data/catalog-queue/hipcomic.json`) before accepting payment, and hide sold books; otherwise Google Merchant Center can treat the feed as misrepresenting availability. The photos are the HipComic sellers' own, so the right to use them needs confirming too. The product page's standing promises (ships in 2 business days, cert-verified, multi-angle photos of the exact copy before dispatch) apply to these listings as well. **Admin › Catalog › Release queue › Pause daily releases** stops further batches at any time.
- Held rows: 2,165 HipComic rows are waiting in `docs/imports/hipcomic-held.csv`, mostly because the title names no publisher (1,306) or no year (1,005). The HipComic listing page has both under "Item Specifics"; a scrape that adds `publisher` and `year` columns lets the same script queue them on its next run.
- Imported sellers: rename/replace the six placeholder seller accounts (`*@sellers.rarecomicscollectors.com`) with real consignors when available; they sign in via "Forgot password".

## 3. Known limitations and recommendations (not yet done)
- Mobile Lighthouse performance sits at 69–78 (home 69, store 69, product 78, collection 72 after round three) because of client-side JavaScript (header, cart, currency providers, store browser); trimming hydration is the next lever. Next built-in polyfills (~14 KB) and the Google tag library (unused JS, external) are the remaining flagged items.
- Knowledge base: 107 articles is the first batch; the JSON import at `/admin/guides/import` is the path to the 10,000-article target. Each batch should keep the same rules (documented facts only, one question per article, links to listings and hubs). Candidate next batches: one article per key issue currently in stock, one per publisher and era covered by the catalogue, and a monthly industry-news entry with dated, sourced events.
- The store page ships every published listing to the browser and filters client-side. That is fine at a few hundred listings; the release queue adds 100 a day, so server-side search and pagination for `/store` should land before the catalogue passes roughly 1,000 listings (about ten days into the schedule).
- Release-queue photos live in the repository (`public/covers/q`, 157 MB for 2,442 listings). Setting `BLOB_READ_WRITE_TOKEN` and moving them to Vercel Blob would keep the repository and builds small as more CSV batches arrive.
- Back/forward cache is disabled by `Cache-Control: no-store` on dynamic HTML.
- Product cover art for the first two imports is the issue's cover scan, not photos of the actual slab (the HipComic queue uses the listing photos); sellers should upload real photos from their dashboard.
- Live Stripe/PayPal have not been exercised with a real charge; the `test` provider covers checkout → refund end-to-end.
- No cookie-consent banner yet: EEA/UK/CH visitors are measured cookielessly (Consent Mode default denied) and never get the chance to opt in; add a small banner that calls `gtag(consent,update,…)` if full analytics coverage in Europe is wanted.
